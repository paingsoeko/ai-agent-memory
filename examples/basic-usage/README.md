# basic-usage

A minimal example project using `@ai-memory/core` directly: remember, dedup, conflict superseding, ingestion, recall with explanations and provenance.

```bash
pnpm build
pnpm --filter example-basic-usage start
```
