# Codex example

`config.toml` shows the MCP server entry and the `notify` hook for automatic ingestion; `AGENTS.md` shows the bootstrap instructions (generate them with `@ai-agent-memory/adapter-codex`).
