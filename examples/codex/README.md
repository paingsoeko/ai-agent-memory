# Codex example

`config.toml` shows the MCP server entry and the `notify` hook for automatic ingestion; `AGENTS.md` shows the bootstrap instructions (generate them with `@local-ai-agent-memory/adapter-codex`).
