# Gemini CLI example

`.gemini/settings.json` registers the MCP server, `.gemini/commands/memory.toml` adds a `/memory <task>` command that shells out to the CLI, and `GEMINI.md` carries the bootstrap instructions (generate with `@ai-agent-memory/adapter-gemini`).
