# OpenCode example

`opencode.json` registers the MCP server, `.opencode/plugin/ai-memory.ts` ingests sessions automatically, `AGENTS.md` holds the bootstrap instructions (generate with `@ai-agent-memory/adapter-opencode`).
